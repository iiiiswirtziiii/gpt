# starz_core/commands/__init__.py
from __future__ import annotations

from discord.ext import commands


def load_all_commands(bot: commands.Bot) -> None:
    """
    Central command loader.

    Keep this as the single place where command packages are registered.
    Each package should expose a `setup(bot)` function.
    """

    # =========================
    # TPS (PrintPos / TP admin)
    # =========================
    try:
        from starz_core.commands.tps import setup as setup_tps
        setup_tps(bot)
    except ModuleNotFoundError:
        print("[COMMANDS] TPS package not found (starz_core/commands/tps). Skipping.")
    except Exception as e:
        print(f"[COMMANDS] TPS setup error: {e}")

    # =========================
    # Add other command packages here later:
    # =========================
    # try:
    #     from starz_core.commands.nukes import setup as setup_nukes
    #     setup_nukes(bot)
    # except Exception as e:
    #     print(f"[COMMANDS] NUKES setup error: {e}")

    print("[COMMANDS] Command loading complete.")
