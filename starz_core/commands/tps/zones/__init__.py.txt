# starz_core/commands/tps/zones/__init__.py
from __future__ import annotations

from discord.ext import commands

from .zone_set import setup as setup_zone_set
from .zone_list import setup as setup_zone_list
from .zone_push import setup as setup_zone_push


def setup(bot: commands.Bot) -> None:
    setup_zone_set(bot)
    setup_zone_list(bot)
    setup_zone_push(bot)
    print("[COMMANDS] Loaded TPS > Zones commands")
